# Requirement Audit

**Task Progress Integrity Note:** The managed task inventory records 48/48 tasks (T-001..T-048) as DONE (100%). The `progress-integrity.json` validity flag is `true` with 0 mismatches. This is bookkeeping evidence only; it does not determine whether source code fulfills each requirement below.

---

| ID | Category | Source | Requirement | Expected Behavior | Acceptance Signal |
|----|----------|--------|-------------|-------------------|-------------------|
| AC-001 | acceptance_criteria | spec.md#21 | Given a source of truth containing valid artifacts and a set of selected targets, when the author runs apply, then the system writes exactly 1 rendered output per artifact for each selected target that natively supports that artifact's type, placed in that target's declared directory. | Apply produces exactly 1 file per supported artifact-target pair, placed in the target's declared directory. No extra or missing files. | `src/lifecycle/executor.ts` routes each pair; conformance fixtures under `conformance-fixtures/` pin per-target output; integration tests assert file-count == 1 per pair. |
| AC-002 | acceptance_criteria | spec.md#22 | Given a selected target whose adapter does not declare native support for a given artifact type, when apply runs on an artifact of that type, then the system skips that artifact-target pair, emits at least 1 warning naming the artifact and the target, and writes 0 files for that pair. | Unsupported artifact-target pairs produce 0 output files and at least 1 named warning. | `src/lifecycle/capability-gate.ts` guards unsupported pairs; unit/integration tests assert 0 writes and warning emission for such pairs. |
| AC-003 | acceptance_criteria | spec.md#23 | Given a selected target identifier that is absent from the target registry, when apply runs, then the system aborts before writing any file and reports the unknown target identifier. | Apply aborts immediately on unknown target identifier; 0 files written before abort. | `src/lifecycle/target-guard.ts` rejects unknown identifiers; integration tests assert early abort and error message containing the identifier. |
| AC-004 | acceptance_criteria | spec.md#34 | Given a prior successful apply with unchanged source artifacts and unchanged configuration, when the author re-runs apply, then every rendered output is byte-identical to the previous run and the system reports 0 changed files. | Re-apply on unchanged inputs produces 0 changed files and byte-identical outputs. | NFR-007 CI artifact (`cross-env-nfr007.json`) confirms 510/510 files byte-identical across two independent runs. NFR-009 CI artifact (`deterministic-render-nfr009.json`) confirms 510/510 files byte-identical across repeated renders. |
| AC-005 | acceptance_criteria | spec.md#35 | Given an existing tool-generated output file whose rendered content would change, when apply overwrites the file, then the system writes at least 1 backup of the prior content before writing the new content. | Content-changing overwrite is preceded by a backup write. Backup file exists before new content is written. | `src/write/backup.ts` and `src/lifecycle/provenance.ts`; integration/safety tests assert backup file present prior to overwrite. |
| AC-006 | acceptance_criteria | spec.md#47 | Given a prior apply recorded in the managed-paths manifest, when the author runs revert, then the system removes exactly the files recorded as tool-generated and leaves every other file untouched. | Revert deletes only manifest-recorded files; all other files in target directory are intact after revert. | `src/lifecycle/revert.ts`; safety tests assert post-revert directory contents match expected non-generated set. |
| AC-007 | acceptance_criteria | spec.md#48 | Given a user-authored native file that the system did not generate residing inside a target directory, when revert runs, then the system deletes 0 such files. | User-authored files in target directories survive revert unchanged. | `src/lifecycle/revert.ts` cross-references `src/manifest/manifest.ts`; safety tests assert 0 user-authored file deletions. |
| AC-008 | acceptance_criteria | spec.md#49 | Given 2 targets that write into 1 shared directory, when the author reverts a single target, then the system removes only that target's recorded files and leaves the sibling target's files present. | Single-target revert in a shared directory leaves sibling target files intact. | `src/manifest/manifest.ts` keys entries by (target-id, path); safety tests for shared-directory revert assert sibling files present after revert. |
| AC-009 | acceptance_criteria | spec.md#60 | Given an artifact declaring neutral behavior keys, when the system renders the artifact for a target, then the emitted artifact contains that target's concrete frontmatter for each key and contains 0 neutral behavior keys. | Emitted artifact has concrete target frontmatter for every declared neutral key; 0 neutral keys appear in output. | `src/vocabulary/translator.ts` and `src/pipeline/stages/stage4-translate.ts`, `stage5-strip.ts`; conformance fixtures contain 0 neutral keys; unit tests assert translated concrete frontmatter. |
| AC-010 | acceptance_criteria | spec.md#61 | Given an artifact carrying a target-specific override for an intent that has no neutral equivalent, when the system renders the artifact for that target, then the emitted artifact contains the override value. | Target-specific override value appears verbatim in emitted frontmatter for that target. | `src/vocabulary/override.ts`; unit tests assert override value propagation for target-specific non-neutral intents. |
| AC-011 | acceptance_criteria | spec.md#62 | Given a declared intent that a target cannot represent, when the system renders for that target, then the system emits at least 1 warning identifying the artifact, the target, and the dropped intent, consistent with the configured lossy-transform policy. | At least 1 warning names the artifact, target, and dropped intent for every non-representable intent. | `src/vocabulary/lossy.ts` and `src/lifecycle/warnings.ts`; unit tests assert warning content for lossy translations. |
| AC-012 | acceptance_criteria | spec.md#72 | Given a skill bundle containing a resource file and an internal reference to that resource, when the system renders the bundle for a target, then the emitted bundle has its path, name, and frontmatter rewritten for that target and its internal reference resolves to the relocated resource. | Emitted skill bundle has rewritten path/name/frontmatter; internal reference points to relocated resource (resolves correctly). | `src/pipeline/stages/stage0-resolve.ts`, `stage1-path.ts`, `stage2-name.ts`; integration tests for skill bundles assert reference resolution. |
| AC-013 | acceptance_criteria | spec.md#73 | Given a skill bundle with an internal reference to a resource that is absent, when the system renders the bundle, then the system emits at least 1 warning naming the unresolved reference rather than shipping a broken link. | At least 1 warning names the unresolved reference; no broken-link artifact is written. | `src/pipeline/bundle.ts`; unit tests assert warning emission for absent bundle resources. |
| AC-014 | acceptance_criteria | spec.md#83 | Given a target whose contract requires a non-Markdown serialization, when the system renders an artifact for that target, then the emitted file is in the required serialization with the artifact's content mapped into that format's fields. | Emitted file is in the target-required non-Markdown format with all content fields correctly mapped. | `src/render/toml.ts`, `src/render/yaml.ts`; conformance fixtures for `toml-command/` and `yaml-recipe/` adapters pin expected format; contract tests assert serialization correctness. |
| AC-015 | acceptance_criteria | spec.md#84 | Given repeated renders of the same unchanged artifact to a structured-format target, when the system serializes the artifact, then the field ordering and quoting are deterministic and the output is byte-identical across runs. | Repeated renders of unchanged structured-format artifacts produce byte-identical output. | NFR-009 CI artifact (`deterministic-render-nfr009.json`): 510/510 files byte-identical, 0 golden mismatches — PASS. |
| AC-016 | acceptance_criteria | spec.md#94 | Given 1 source command artifact and 3 targets whose native slots differ, when apply runs, then the system emits the artifact as a command on the command-supporting target, as a skill on the skill-supporting target, and as an agent definition on the agent-supporting target. | Deployment-type routing maps 1 source to 3 different on-disk shapes depending on each target's native slot. | `src/pipeline/stages/stage8-route.ts`; integration tests for deployment-type routing assert correct per-target shape. |
| AC-017 | acceptance_criteria | spec.md#95 | Given a source artifact whose resolved deployment type has 0 native slots on a selected target, when apply runs, then the system skips that pair and emits at least 1 warning. | Artifact-target pairs with no native slot are skipped; at least 1 warning emitted. | `src/lifecycle/capability-gate.ts`; unit tests assert skip + warning for 0-slot deployment type. |
| AC-018 | acceptance_criteria | spec.md#106 | Given the dry-run flag on apply, when the author runs apply, then the system reports every file it would create, overwrite, back up, or remove through reconciliation, writes 0 files to disk, and deletes 0 files from disk. | Dry-run apply reports all planned operations; 0 bytes written or deleted. | `src/lifecycle/dry-run.ts`; e2e tests assert no FS changes after dry-run apply and complete operation report in output. |
| AC-019 | acceptance_criteria | spec.md#107 | Given the dry-run flag on revert, when the author runs revert, then the system reports every file it would remove and deletes 0 files from disk. | Dry-run revert reports all planned removals; 0 files deleted. | `src/lifecycle/dry-run.ts`; e2e tests assert no FS changes after dry-run revert and removal report in output. |
| AC-020 | acceptance_criteria | spec.md#118 | Given an artifact with malformed frontmatter, when discovery runs, then the system emits at least 1 validation warning naming the file, drops that artifact from the run, and processes the remaining artifacts without terminating. | Malformed-frontmatter artifact produces at least 1 named warning, is dropped, and remaining artifacts continue processing. | `src/discovery/drop-and-warn.ts`; unit tests inject malformed artifacts and assert warning + continuation behavior. |
| AC-021 | acceptance_criteria | spec.md#119 | Given an artifact whose frontmatter fails the schema for its artifact type, when discovery runs, then the system reports the failing field and excludes the artifact from rendering. | Schema-failing artifact is excluded; failing field is named in output. | `src/discovery/schemas.ts` and `src/discovery/classify.ts`; unit tests assert field-level error reporting and exclusion. |
| AC-022 | acceptance_criteria | spec.md#129 | Given a configuration that enables a subset of targets and artifact types, when apply runs, then the system distributes only the enabled artifact types to the enabled targets. | Only configuration-enabled artifact types reach configuration-enabled targets; others are excluded. | `src/config/selection.ts`; integration tests with subset config assert only enabled pairs are written. |
| AC-023 | acceptance_criteria | spec.md#130 | Given a configuration file containing an unknown key, when the system loads the configuration, then the system rejects the configuration and reports the unknown key rather than ignoring the key. | Unknown config key causes rejection; key name appears in error output. | `src/config/schema.ts`; unit tests assert rejection and unknown-key reporting. |
| AC-024 | acceptance_criteria | spec.md#141 | Given a new target described by a declarative adapter descriptor and a passing conformance test, when the contributor adds the target to the registry, then the system distributes to the new target with 0 changes to core transformation logic. | Adding a new declarative descriptor + conformance test enables distribution to a new target with 0 core code changes. | `src/registry/register.ts`, `src/registry/builtin.ts`; conformance fixtures under `conformance-fixtures/` demonstrate 40 targets added declaratively. |
| AC-025 | acceptance_criteria | spec.md#142 | Given a target adapter that has 0 passing conformance tests, when the registry is validated, then the system marks that target as not conformance-verified. | Targets with 0 passing conformance tests carry `conformance-verified: false` in registry metadata. | `src/registry/conformance-status.ts`; unit tests assert `not-conformance-verified` status for targets with 0 passing tests. |
| AC-026 | acceptance_criteria | spec.md#153 | Given a computed output path that resolves inside the project root, when the system writes the file, then the write proceeds. | Write operations whose paths resolve inside project root proceed normally. | `src/write/containment.ts`; safety tests assert successful write for in-root paths. |
| AC-027 | acceptance_criteria | spec.md#154 | Given a computed write-or-delete path that resolves outside the project root, including a path that escapes via a symlink, when the system attempts the operation, then the system refuses the operation and reports the escaping path. | Write or delete to out-of-root path (including symlink escape) is refused; escaping path named in output. | `src/write/containment.ts`; safety tests with symlink escape paths assert refusal and path reporting. |
| AC-028 | acceptance_criteria | spec.md#143 | Given a remote catalog input that is unreachable or fails validation, when apply runs, then the system falls back to the built-in registry and completes the run. | Unreachable/invalid remote catalog causes fallback to built-in registry; run completes. | `src/registry/catalog.ts`; classified `deferred-automation` in coverage-map.md — automated when catalog seam is built. No MVP requirement depends on this. |
| AC-029 | acceptance_criteria | spec.md#24 | Given a source of truth containing 0 discoverable artifacts, when apply runs, then the system completes with an empty-run report and writes 0 files. | Zero-artifact source yields 0 files written and an empty-run report. | `src/discovery/empty-run.ts`; unit/integration tests for empty source of truth assert 0 writes and empty-run report. |
| AC-030 | acceptance_criteria | spec.md#108 | Given a dry-run apply where reconciliation would remove 1 or more orphaned outputs, when the author runs apply, then the reconcile deletions appear in the preview labeled as removals and 0 files are deleted. | Dry-run apply labels reconcile deletions as removals in preview; 0 files deleted. | `src/lifecycle/dry-run.ts` and `src/lifecycle/reconcile.ts`; e2e tests assert removal labels in dry-run output and 0 actual deletions. |
| AC-031 | acceptance_criteria | spec.md#36 | Given a prior apply and a subsequent run that no longer produces a previously generated output, when the author re-runs apply, then the system removes only the orphaned outputs recorded in the managed-paths manifest and removes 0 files absent from the manifest. | Re-apply removes only manifest-recorded orphans; non-manifest files are untouched. | `src/lifecycle/reconcile.ts` and `src/manifest/manifest.ts`; safety tests assert orphan-only removal and 0 non-manifest deletions. |
| AC-032 | acceptance_criteria | spec.md#50 | Given a managed-paths manifest that is absent, unreadable, or fails its integrity check, when the author runs revert or a reconciling apply, then the system aborts the deletion path, reports the manifest error, and deletes 0 files. | Missing/corrupt manifest aborts revert and reconciliation; 0 files deleted; manifest error reported. | `src/manifest/integrity.ts`; safety tests inject missing/corrupt manifests and assert abort + error report + 0 deletions. |
| AC-033 | acceptance_criteria | spec.md#37 | Given a content-changing overwrite, when the system writes the backup, then the backup path resolves inside the project root, is excluded from artifact discovery on subsequent runs, and no more than 3 backups per file are retained. | Backup is inside project root, excluded from discovery, and at most 3 backups retained per file. | `src/write/backup-location.ts` and `src/write/backup.ts`; unit tests assert in-root backup path, discovery exclusion, and 3-backup retention cap. |
| AC-034 | acceptance_criteria | spec.md#96 | Given an artifact declaring a neutral execution intent, when the system resolves the deployment type, then the resolved deployment type equals the deployment type mapped from that declared execution intent, and an artifact declaring no execution intent resolves to the deployment type mapped from its artifact type. | Deployment-type resolution maps execution intent → deployment type; artifacts with no execution intent fall back to artifact-type mapping. | `src/lifecycle/planner.ts` and `src/pipeline/stages/stage8-route.ts`; unit tests verify both intent-present and intent-absent resolution paths. |
| AC-035 | acceptance_criteria | spec.md#131 | Given an effective configuration that selects 0 targets, when apply runs, then the system completes as a no-op, reports that 0 targets were selected, and writes 0 files. | Zero-target configuration produces 0 writes and a no-op report naming 0 selected targets. | `src/lifecycle/no-op-detect.ts`; integration tests for zero-target config assert 0 writes and no-op report. |
| FR-001 | functional | spec.md#162 | The system SHALL classify each discovered artifact into exactly 1 of the 4 artifact types {rule, skill, subagent, command}, producing a typed artifact list. | Every discovered artifact is assigned exactly 1 type from {rule, skill, subagent, command}. | `src/discovery/classify.ts`; unit tests assert each artifact type is correctly classified. |
| FR-001..FR-065 | coverage_meta | coverage-map.md#3 | Coverage-map header row: every FR-001..FR-065 and NFR-001..NFR-012 is mapped to an automated test approach. 0 rows are manual; deferred-automation only for FR-041/AC-028. | Meta-row documenting test-coverage mapping scope; not an individual functional requirement. | This is a header entry from coverage-map.md describing the test-coverage inventory scope. No individual implementation evidence is expected for this meta-row. |
| FR-002 | functional | spec.md#174 | The system SHALL parse each discovered artifact classified by FR-001 into exactly 1 frontmatter map paired with exactly 1 Markdown body. | Each artifact produces exactly 1 frontmatter map and 1 Markdown body after parsing. | `src/discovery/parse.ts`; unit tests assert frontmatter + body decomposition for each artifact type. |
| FR-003 | functional | spec.md#178 | The system SHALL validate each artifact's frontmatter against exactly 1 schema selected by artifact type (FR-001) before rendering. | Per-type schema validation runs on every artifact before any rendering stage executes. | `src/discovery/schemas.ts`; unit tests assert per-type schema selection and validation before pipeline entry. |
| FR-004 | functional | spec.md#186 | When an artifact's frontmatter is malformed, the system SHALL emit at least 1 validation warning that names the file and drop that artifact from the run. | Malformed frontmatter → at least 1 warning naming the file → artifact excluded from run. | `src/discovery/drop-and-warn.ts`; unit tests inject malformed artifacts and assert named warning + exclusion. |
| FR-005 | functional | spec.md#190 | The system SHALL report 100% of a run's validation warnings without aborting on the first invalid artifact. | All validation warnings are collected and reported; run continues past first invalid artifact. | `src/discovery/discover.ts`; unit tests with multiple malformed artifacts assert all warnings reported and run continues. |
| FR-006 | functional | spec.md#197 | When registering a target, the system SHALL represent that target as exactly 1 declarative adapter descriptor rather than as imperative per-target code, producing 1 auditable descriptor per target. | Each target is registered via a single declarative descriptor file, not imperative code. | `src/registry/adapters/` cluster directories each contain a declarative descriptor; `src/registry/register.ts` parses descriptors; no per-target imperative registration code present. |
| FR-007 | functional | spec.md#217 | When publishing a target-registry release, the system SHALL assign exactly 1 version identifier to that release, producing an auditable registry version. | Each registry release carries exactly 1 version identifier recorded in release metadata. | `src/registry/version.ts`; unit tests assert single version identifier per registry build. |
| FR-008 | functional | spec.md#221 | The system SHALL admit a new target from exactly 1 declarative adapter descriptor without changes to core transformation logic. | Adding a new descriptor file is sufficient to register a new target; no core transformation code changes needed. | `src/registry/register.ts` dynamically loads descriptors; conformance fixtures demonstrate 40 registered targets without core changes. |
| FR-009 | functional | spec.md#225 | The system SHALL withhold conformance-verified status from a target until at least 1 conformance test pinning that target's expected on-disk output passes. | A target's `conformance-verified` flag remains false until its golden-fixture conformance test passes. | `src/registry/conformance-status.ts` and `src/registry/parity.ts`; conformance fixtures under `conformance-fixtures/` with 40 directories; unit tests assert status gating. |
| FR-010 | functional | spec.md#233 | When the transformation stage queries a target, the system SHALL expose 100% of that target's native-support capability flags, producing queryable capability data. | All capability flags declared in a target descriptor are accessible to the transformation stage. | `src/registry/registry.ts`; unit tests assert full capability-flag exposure per target. |
| FR-011 | functional | spec.md#240 | When transforming an artifact-target pair, the system SHALL execute the transformation pipeline as exactly 1 fixed sequence of 8 ordered stages, applying each stage exactly 1 time, producing 1 fully transformed artifact. | Pipeline executes exactly 8 stages in fixed order, each exactly once, for each artifact-target pair. | `src/pipeline/runner.ts`; `src/pipeline/stages/` contains stage0..stage8; unit tests assert stage order and single application. |
| FR-012 | functional | spec.md#244 (stage 1 of ordered-stages clause) | Stage 1 of the 8-stage pipeline: path rewrite. The system SHALL rewrite 100% of intra-artifact and intra-bundle path references. | All intra-artifact and intra-bundle path references are rewritten for the target before subsequent stages run. | `src/pipeline/stages/stage1-path.ts`; unit tests for path rewriting assert 100% reference coverage. |
| FR-013 | functional | spec.md#244 (stage 2 of ordered-stages clause) | Stage 2 of the 8-stage pipeline: name rewrite. The system SHALL compute exactly 1 on-disk name per target for each artifact according to that target's naming rule. | Each artifact receives exactly 1 target-specific on-disk name. | `src/pipeline/stages/stage2-name.ts` and `src/pipeline/naming.ts`; unit tests assert one name per artifact per target. |
| FR-014 | functional | spec.md#244 (stage 3 of ordered-stages clause) | Stage 3 of the 8-stage pipeline: argument-placeholder rewrite. The system SHALL rewrite 100% of argument placeholders in a command artifact to the target's argument token. | All argument placeholders in command artifacts are replaced with the target's declared argument token. | `src/pipeline/stages/stage3-args.ts`; unit tests assert 100% placeholder rewriting. |
| FR-015 | functional | spec.md#244 (stage 4 of ordered-stages clause) | Stage 4 of the 8-stage pipeline: neutral-behavior translation. The system SHALL translate 100% of declared neutral behavior keys into the target's concrete frontmatter. | Every declared neutral behavior key becomes a concrete target-native frontmatter entry. | `src/pipeline/stages/stage4-translate.ts` and `src/vocabulary/translator.ts`; unit/conformance tests assert 0 neutral keys remain after translation. |
| FR-016 | functional | spec.md#269 | The system SHALL provide exactly 1 target-specific override mechanism for intent that has no neutral-vocabulary equivalent. | A single override mechanism exists for target-specific intent with no neutral equivalent. | `src/vocabulary/override.ts`; unit tests assert override mechanism applies target-specific non-neutral values. |
| FR-017 | functional | spec.md#277 | The system SHALL apply to skill and subagent artifacts the same 4 transformation categories — path, name, frontmatter, and format — that command artifacts receive. | Skills and subagents go through path, name, frontmatter, and format transformation stages identically to commands. | `src/pipeline/runner.ts` applies the same stage sequence to all artifact types; integration tests assert skill/subagent output equivalence to command transformation. |
| FR-018 | functional | spec.md#281 | When a declared intent cannot be represented on a target, the system SHALL emit at least 1 warning identifying the artifact, the target, and the dropped intent. | Non-representable intent produces at least 1 warning naming artifact, target, and dropped intent. | `src/lifecycle/warnings.ts` and `src/vocabulary/lossy.ts`; unit tests assert warning content (artifact + target + intent). |
| FR-019 | functional | spec.md#281 (FR-019 clause within FR-018 block) | The system SHALL NOT discard non-representable intent for a target without emitting at least 1 warning. | Dropping non-representable intent without warning is prohibited. | `src/vocabulary/lossy.ts`; unit tests confirm warning is always emitted before any lossy drop. |
| FR-020 | functional | spec.md#244 (stage 7 of ordered-stages clause) | Stage 7 of the 8-stage pipeline: format conversion. The system SHALL emit each artifact in exactly 1 serialization format required by its target, producing a target-loadable file. | Each artifact is serialized into the single format declared by the target descriptor. | `src/pipeline/stages/stage7-format.ts` and `src/render/`; conformance fixtures for toml-command, yaml-recipe verify format output. |
| FR-021 | functional | spec.md#296 | When an unchanged artifact is rendered more than once, the system SHALL keep the renders 100% byte-identical, producing 0 spurious differences. | Repeated renders of unchanged artifacts produce identical bytes. | NFR-007 CI artifact: 510/510 files byte-identical. NFR-009 CI artifact: 510/510 files byte-identical, 0 golden mismatches. Both PASS. |
| FR-022 | functional | spec.md#300 | When a target requires companion files, the system SHALL write 100% of them alongside the primary rendered output, producing a complete artifact set. | All companion files declared by a target are written alongside primary output. | `src/render/companions.ts`; conformance fixtures under `companion-file/` adapter; integration tests assert companion file presence. |
| FR-023 | functional | spec.md#244 (stage 8 of ordered-stages clause) | Stage 8 of the 8-stage pipeline: deployment-type routing. The system SHALL place each artifact in exactly 1 native slot per target selected by the resolved deployment type. | Each artifact lands in exactly 1 target-native slot based on FR-048-resolved deployment type. | `src/pipeline/stages/stage8-route.ts`; unit/integration tests assert single-slot routing per deployment type. |
| FR-024 | functional | spec.md#315 | When the system writes a generated file, the system SHALL record 100% of generated files in the managed-paths manifest keyed by the pair (target identifier, path), producing an auditable provenance record. | Every written file is recorded in the manifest under its (target-id, path) key before the run ends. | `src/manifest/manifest.ts` and `src/lifecycle/provenance.ts`; integration tests assert 100% manifest coverage of written files. |
| FR-025 | functional | spec.md#319 | Before overwriting a content-changing tool-generated file recorded by FR-024, the system SHALL write at least 1 backup of the prior content. | A backup exists before any content-changing overwrite executes. | `src/write/backup.ts`; safety tests verify backup write precedes overwrite in all content-changing scenarios. |
| FR-026 | functional | spec.md#335 | The system SHALL confirm that 100% of write paths resolve inside the project root before writing. | Every write path is containment-checked against the project root before any write operation. | `src/write/containment.ts`; safety tests assert containment check on every write path. |
| FR-027 | functional | spec.md#339 | When a write or delete resolves to a path that escapes the project root, including via a symlink, the system SHALL refuse the operation and report the escaping path, producing a refusal message. | Escaping paths (including symlink escapes) are refused; the path is named in the refusal message. | `src/write/containment.ts`; safety tests with symlink escape scenarios assert refusal + path report. |
| FR-028 | functional | spec.md#343 | On re-apply, the system SHALL reconcile 100% of the previously generated outputs that the current run no longer produces. | Every orphaned manifest entry (not produced by current run) is removed during re-apply. | `src/lifecycle/reconcile.ts`; integration tests assert 100% orphan removal during re-apply. |
| FR-029 | functional | spec.md#358 | When a run starts, the system SHALL load exactly 1 effective run configuration selecting the active targets and artifact types, producing the active target and artifact-type set. | Exactly 1 effective configuration is loaded at run start, determining active targets and artifact types. | `src/config/load.ts`; unit tests assert single-configuration loading and effective set derivation. |
| FR-030 | functional | spec.md#366 | When loading configuration (FR-029) that contains at least 1 unknown key, the system SHALL reject the configuration and report each unknown key, producing a rejection message. | Unknown config keys cause hard rejection; each unknown key named in rejection output. | `src/config/schema.ts`; unit tests assert rejection message containing each unknown key. |
| FR-031 | functional | spec.md#370 | The system SHALL resolve configuration from 3 sources — project-level, ancestor-directory, and global — in a fixed precedence where project-level overrides ancestor-directory and ancestor-directory overrides global. | Configuration merges 3 sources in fixed precedence: project > ancestor > global. | `src/config/precedence.ts`; unit tests assert correct precedence for all 3 source combinations. |
| FR-032 | functional | spec.md#374 | The system SHALL let each command-line target-selection override replace exactly 1 corresponding file-configuration value. | Each CLI target-selection flag replaces its corresponding file-config value. | `src/config/cli-override.ts`; unit tests assert CLI override applies to exactly 1 corresponding config value. |
| FR-033 | functional | spec.md#385 | The system SHALL provide exactly 1 apply operation that renders and writes selected artifacts to every selected supporting target, producing 1 rendered output per supported artifact-target pair. | Exactly 1 apply command exists; it produces 1 output per supported pair. | `src/cli/index.ts` and `src/lifecycle/run.ts`; e2e tests assert single apply entry point and 1 output per pair. |
| FR-034 | functional | spec.md#351 (FR-050 block) | When the author runs revert, the system SHALL remove only the tool-generated files recorded by FR-024. | Revert removes only manifest-recorded files; all others untouched. | `src/lifecycle/revert.ts` cross-referenced with `src/manifest/manifest.ts`; safety tests assert manifest-guarded deletion. |
| FR-035 | functional | spec.md#401 | The system SHALL NOT delete a file during revert unless the manifest records that file as tool-generated. | No revert deletion occurs for files absent from the manifest. | `src/lifecycle/revert.ts`; safety tests with non-manifest files assert 0 deletions for those files. |
| FR-036 | functional | spec.md#405 | During revert of 1 target, the system SHALL leave intact 100% of the FR-024 recorded files of any other target sharing the same directory. | Sibling-target manifest entries survive single-target revert without modification. | `src/manifest/manifest.ts` per-target keying; safety tests for shared-directory scenarios assert sibling-target files intact. |
| FR-037 | functional | spec.md#409 | The system SHALL provide exactly 1 dry-run mode for apply that reports every create, overwrite, backup, and reconcile-removal and writes 0 files to disk. | Exactly 1 dry-run apply mode exists; it reports all planned operations and writes 0 bytes. | `src/lifecycle/dry-run.ts`; e2e tests assert 0 FS changes and full operation report. |
| FR-038 | functional | spec.md#413 | The system SHALL provide exactly 1 dry-run mode for revert that reports every planned removal and deletes 0 files from disk. | Exactly 1 dry-run revert mode exists; it reports all planned removals and deletes 0 files. | `src/lifecycle/dry-run.ts`; e2e tests assert 0 FS deletions and planned-removal report. |
| FR-039 | functional | spec.md#417 | When a selected target's adapter does not declare the FR-047 capability flag for an artifact type, the system SHALL skip that pair, write 0 files for it, and emit at least 1 warning. | Unsupported artifact-type/target pairs are skipped with 0 writes and at least 1 warning. | `src/lifecycle/capability-gate.ts`; unit tests inject missing capability flags and assert skip + warning. |
| FR-040 | functional | spec.md#421 | When a selected target identifier is absent from the registry, the system SHALL abort the run before writing any file and report the unknown identifier. | Unknown target identifier causes pre-write abort; identifier named in error. | `src/lifecycle/target-guard.ts`; integration tests assert abort before any write and identifier in error message. |
| FR-041 | functional | spec.md#425 | The system SHOULD validate any remote catalog input before use and fall back to the built-in registry when the catalog is unavailable. | Remote catalog validation and fallback to built-in registry when unavailable. | `src/registry/catalog.ts`; classified `deferred-automation` in coverage-map.md (Post-MVP; automated when catalog seam is built). |
| FR-042 | functional | spec.md#244 (stage 5 of ordered-stages clause) | Stage 5 of the 8-stage pipeline: neutral-behavior strip. The system SHALL strip 100% of neutral behavior keys so 0 neutral behavior keys appear in any emitted file. | Zero neutral keys appear in any emitted artifact after this stage. | `src/pipeline/stages/stage5-strip.ts`; conformance fixtures contain 0 neutral keys; unit tests assert complete stripping. |
| FR-043 | functional | spec.md#244 (stage 6 of ordered-stages clause) | Stage 6 of the 8-stage pipeline: frontmatter rewrite. The system SHALL apply all 3 frontmatter rule categories — strip, passthrough, and inject — to produce the emitted frontmatter. | Emitted frontmatter reflects application of strip, passthrough, and inject rules from the target descriptor. | `src/pipeline/stages/stage6-frontmatter.ts`; conformance fixtures pin expected frontmatter per target; unit tests assert all 3 rule categories applied. |
| FR-044 | functional | spec.md#201 | Each target adapter descriptor SHALL declare exactly 1 destination directory for the target. | Every target descriptor contains exactly 1 destination directory declaration. | `src/registry/adapters/` descriptors; `src/registry/registry.ts` schema validation; unit tests assert exactly 1 destination directory per descriptor. |
| FR-045 | functional | spec.md#205 | Each target adapter descriptor SHALL declare exactly 1 serialization format and exactly 1 argument token for the target. | Every target descriptor has exactly 1 format and exactly 1 argument token. | `src/registry/adapters/` descriptor schemas; unit tests assert format and token uniqueness per descriptor. |
| FR-046 | functional | spec.md#209 | Each target adapter descriptor SHALL declare exactly 3 frontmatter rule categories: strip, passthrough, and inject. | Every target descriptor has all 3 frontmatter categories: strip, passthrough, inject. | `src/registry/adapters/build.ts` and descriptor schema; unit tests assert all 3 frontmatter categories present in every descriptor. |
| FR-047 | functional | spec.md#213 | Each target adapter descriptor SHALL declare at least 1 native-support capability flag per artifact type. | Every target descriptor declares capability flags for all 4 artifact types. | `src/registry/adapters/` descriptor schema; unit tests assert capability flag completeness per descriptor. |
| FR-048 | functional | spec.md#304 | When resolving deployment type, the system SHALL map each artifact to exactly 1 deployment type of the 3 types {command, skill, agent}, taken from the artifact's declared execution intent when present and from the artifact type otherwise, producing 1 resolved deployment type. | Deployment-type resolution produces exactly 1 type per artifact; intent-declared takes precedence over artifact-type fallback. | `src/lifecycle/planner.ts`; unit tests assert both resolution paths (intent-present and intent-absent). |
| FR-049 | functional | spec.md#327 | The system SHALL retain no more than 3 backups per overwritten file (FR-025), deleting the oldest surplus backup rather than letting backups accumulate without bound. | Maximum 3 backups per file; oldest backup deleted when 4th would be created. | `src/write/backup.ts`; unit tests assert 3-backup cap and oldest-first eviction. |
| FR-050 | functional | spec.md#351 | When the managed-paths manifest is absent, unreadable, or fails its integrity check, the system SHALL abort revert (FR-034) and reconciliation (FR-028), report the manifest error, and delete 0 files. | Missing/corrupt manifest aborts all deletion paths; error reported; 0 files deleted. | `src/manifest/integrity.ts`; safety tests inject missing/corrupt manifests and assert abort + error + 0 deletions. |
| FR-051 | functional | spec.md#347 | During reconciliation the system SHALL remove only the files recorded by FR-024 in the manifest. | Reconciliation deletes only manifest-recorded entries; no other files touched. | `src/lifecycle/reconcile.ts`; safety tests assert 0 non-manifest deletions during reconciliation. |
| FR-052 | functional | spec.md#166 | The system SHALL exclude and report any artifact that matches 0 types or more than 1 type. | Ambiguous or unclassifiable artifacts are excluded from the run and reported. | `src/discovery/classify.ts`; unit tests inject 0-match and multi-match artifacts and assert exclusion + report. |
| FR-053 | functional | spec.md#170 | An empty source of truth SHALL yield a run that writes 0 files. | Empty source directory produces 0 written files and completes normally. | `src/discovery/empty-run.ts`; unit/integration tests assert 0 writes for empty source. |
| FR-054 | functional | spec.md#362 | An effective configuration that selects 0 targets SHALL yield a no-op run that writes 0 files. | Zero-target configuration produces 0 writes; run completes as no-op. | `src/lifecycle/no-op-detect.ts`; integration tests for zero-target config assert 0 writes. |
| FR-055 | functional | spec.md#331 | The system SHALL exclude the backup location from artifact discovery. | The backup directory is not scanned during artifact discovery. | `src/write/backup-location.ts` used in `src/discovery/walk.ts`; unit tests assert backup location excluded from walk. |
| FR-056 | functional | spec.md#323 | The system SHALL NOT overwrite a tool-generated file before that file's prior-content backup from FR-025 is written. | Overwrite is sequenced after backup write completes; no partial-overwrite scenario without backup. | `src/write/guarded-fs.ts`; safety tests assert backup-then-write ordering under failure injection. |
| FR-057 | functional | spec.md#182 | The system SHALL NOT render an artifact that fails parsing (FR-002) or schema validation (FR-003). | Parse-failing or schema-failing artifacts are excluded before any rendering stage. | `src/discovery/discover.ts` gates on parse + schema success; unit tests assert render exclusion for failing artifacts. |
| FR-058 | functional | spec.md#229 | The system SHALL NOT mark a target conformance-verified while 0 of its conformance tests pass. | A target's conformance-verified flag cannot be set true while its test count is 0. | `src/registry/conformance-status.ts`; unit tests assert flag stays false with 0 passing tests. |
| FR-059 | functional | spec.md#273 | The system SHALL NOT reorder or skip any of the 8 transformation stages defined in FR-011. | The stage sequence is immutable; no conditional skipping or reordering is possible. | `src/pipeline/runner.ts` hardcodes stage sequence; unit tests execute the pipeline and assert all 8 stages fire in order. |
| FR-060 | functional | spec.md#378 | During apply, the system SHALL NOT distribute an artifact type that the effective configuration from FR-029 has not enabled. | Non-enabled artifact types are never distributed regardless of source content. | `src/config/selection.ts`; integration tests with disabled artifact types assert 0 writes for those types. |
| FR-061 | functional | spec.md#389 | During apply, the system SHALL NOT modify a user-authored native file that FR-024 has not recorded as tool-generated. | User-authored files (not in manifest) are never overwritten by apply. | `src/write/guarded-fs.ts` checks manifest before write; safety tests assert non-manifest files remain unmodified. |
| FR-062 | functional | spec.md#393 | The system SHALL NOT write more than 1 rendered output per artifact-target pair routed by FR-023. | Exactly 1 output is written per artifact-target pair; duplicate writes are prohibited. | `src/lifecycle/executor.ts`; unit tests assert deduplication of artifact-target pairs before write. |
| FR-063 | functional | spec.md#429 | During a dry-run apply, the system SHALL NOT delete any file (FR-037). | Dry-run apply produces 0 file deletions regardless of planned reconcile removals. | `src/lifecycle/dry-run.ts`; e2e tests assert 0 deletions after dry-run apply including reconcile scenarios. |
| FR-064 | functional | spec.md#433 | The system SHALL NOT distribute to a target that is absent from the registry (FR-040). | Distribution to unregistered targets is blocked before any write. | `src/lifecycle/target-guard.ts`; integration tests assert distribution blocked for unregistered targets. |
| FR-065 | functional | spec.md#437 | The system SHALL NOT emit an output outside a target's declared directory (FR-044). | All outputs are confined to the target's declared directory; no out-of-directory writes. | `src/write/containment.ts` and `src/pipeline/stages/stage1-path.ts`; safety tests assert path confinement. |
| NFR-001 | non_functional | spec.md#444 | A no-op re-apply of FR-033 SHALL keep 100% of rendered outputs byte-identical and report 0 changed files. | Re-apply on unchanged inputs reports 0 changed files; all outputs are byte-identical. | NFR-007 CI artifact (`cross-env-nfr007.json`): 510/510 files byte-identical across two independent runs — PASS. |
| NFR-001..NFR-012 | coverage_meta | coverage-map.md#3 | Coverage-map header row: every NFR-001..NFR-012 is mapped to an automated test approach. 0 rows are manual; deferred-automation only for FR-041/AC-028. | Meta-row documenting test-coverage mapping scope; not an individual non-functional requirement. | This is a header entry from coverage-map.md describing the test-coverage inventory scope. No individual implementation evidence is expected for this meta-row. |
| NFR-002 | non_functional | spec.md#447 | 100% of content-changing overwrites SHALL be preceded by at least 1 backup of the prior content (FR-025). | Every content-changing overwrite is backed up; 0 unprotected overwrites. | `src/write/backup.ts` and `src/write/guarded-fs.ts`; safety tests assert 100% backup coverage across all overwrite scenarios. |
| NFR-003 | non_functional | spec.md#450 | 100% of write and delete operations SHALL pass a project-root containment check (FR-026, FR-027) before execution. | Every write/delete is containment-checked before execution; 0 unchecked operations. | `src/write/containment.ts`; safety tests assert containment check invoked for 100% of write/delete operations. |
| NFR-004 | non_functional | spec.md#453 | Every conformance-verified target (FR-009) SHALL have at least 1 passing per-target conformance test. | No target is marked conformance-verified without a passing golden-fixture test. | 40 target directories under `conformance-fixtures/` (10 markdown-frontmatter + 26 markdown-longtail + 2 toml-command + 1 yaml-recipe + 1 companion-file) each have pinned fixture tests. All 40 exceed the NFR-008 threshold of 35. |
| NFR-005 | non_functional | spec.md#456 | The system SHALL complete distribution of at least 100 artifacts across at least 30 targets in under 30 seconds on a developer workstation. | 100 artifacts × 30 targets completes in ≤ 30 seconds. | CI artifact `test-results/benchmark-nfr005.json`: 100 artifacts × 30 targets = 821ms elapsed, threshold 30,000ms — PASS. |
| NFR-006 | non_functional | spec.md#459 | The system SHALL surface at least 1 warning for every skipped or lossy transformation (FR-018, FR-039), naming the artifact and the target. | Zero silent skips or silent capability losses in any run. | `src/lifecycle/warnings.ts`; unit tests assert warning presence for every skip and every lossy translation scenario. |
| NFR-007 | non_functional | spec.md#462 | The system SHALL keep a manual run and a CI run on identical inputs 100% byte-identical. | Manual and CI runs on the same inputs produce byte-identical output. | CI artifact `test-results/cross-env-nfr007.json`: 510/510 files byte-identical across two independent runs — PASS. |
| NFR-008 | non_functional | spec.md#465 | The system SHALL support at least 35 target adapters, the pinned Ruler-parity baseline whose exact Ruler reference version is recorded per release. | At least 35 target adapters supported; Ruler reference version recorded. | Conformance fixtures exist for 40 targets (10 markdown-frontmatter + 26 markdown-longtail + 2 toml-command + 1 yaml-recipe + 1 companion-file). 40 ≥ 35 — threshold met. |
| NFR-009 | non_functional | spec.md#468 | Structured-format serialization SHALL keep field ordering and quoting 100% byte-identical across repeated renders of unchanged input. | Repeated structured-format renders of unchanged input are byte-identical; 0 field-ordering differences. | CI artifact `test-results/deterministic-render-nfr009.json`: 510/510 files byte-identical, 0 golden mismatches — PASS. |
| NFR-010 | non_functional | spec.md#471 | A single malformed artifact SHALL NOT abort a run, and the run SHALL complete for 100% of valid artifacts. | Malformed artifact presence does not abort the run; all valid artifacts are processed. | `src/discovery/drop-and-warn.ts`; unit tests confirm run-completion for 100% of valid artifacts when malformed artifacts are present. |
| NFR-011 | non_functional | spec.md#474 | Every release SHALL record exactly 1 target-registry version identifier so contract changes are traceable. | Each release carries exactly 1 registry version identifier in release metadata. | `src/registry/version.ts`; release process / build output; unit tests assert single version identifier per registry release. |
| NFR-012 | non_functional | spec.md#477 | Every managed-paths manifest write from FR-024 SHALL complete as exactly 1 atomic operation, leaving 0 partially written manifests under interrupted or concurrent runs. | Manifest writes are atomic; 0 partial manifests are possible. | `src/manifest/manifest.ts`; safety tests simulate interruption and assert no partial manifests. |
