# SPEC-GUARD Fallback Judgments

| ID | Status | Evidence Summary | Notes |
|----|--------|-----------------|-------|
| AC-004 | IMPLEMENTED | `src/lifecycle/no-op-detect.ts:isIdempotentNoOp` detects zero changed files; `tests/integration/lifecycle.test.ts:"AC-004/NFR-001: a no-op re-apply reports 0 changed files, byte-identical"` verifies `r2.changedFiles === 0` and byte-identical content after re-apply. Evidence kind: `source_and_test`. | Integration test directly exercises the acceptance criterion on a real FS temp root. No inference required. |
| AC-015 | IMPLEMENTED | `src/render/order.ts:canonicalOrder` enforces PREFERRED_ORDER then stable alphabetical sort for remaining keys, producing deterministic field ordering on every serialize call; `tests/e2e/deterministic-render.test.ts` confirms 510/510 byte-identical structured outputs across two independent runs (measured artifact `test-results/deterministic-render-nfr009.json`, `pass:true`). Evidence kind: `measured_runtime` (overlapping NFR-009 artifact). | The NFR-009 measured artifact covers structured-format targets and directly satisfies this acceptance criterion's byte-identity gate. |
| AC-028 | IMPLEMENTED | `src/registry/catalog.ts:loadCatalogOrFallback` catches both thrown errors and failed Zod schema parses, returning the built-in `ALL_DESCRIPTORS` set with `usedFallback:true`; `tests/contract/catalog-fallback.test.ts` exercises the unreachable-catalog path (`throw new Error('network down')`) and the invalid-schema path, asserting `usedFallback === true` and `descriptors.length >= 35`. Evidence kind: `unit_test`. | FR-041 priority is Nice-to-Have/Post-MVP; the SHOULD wording is advisory. The fallback seam is fully wired and the contract test confirms the run completes over the built-in registry. Coverage-map "deferred-automation" note refers to broader catalog validation automation, not to the fallback behavior itself, which is present and tested. |
| FR-021 | IMPLEMENTED | `src/render/order.ts:canonicalOrder` ensures deterministic key ordering; `src/lifecycle/no-op-detect.ts:changedFileCount` filters writes by `changeType !== 'unchanged'`, preventing unnecessary re-writes; `tests/e2e/deterministic-render.test.ts` measured artifact confirms 510/510 byte-identical structured outputs (NFR-009 artifact, `pass:true`). Evidence kind: `measured_runtime`. | The measured artifact was produced by running `apply` twice over identical inputs and hashing every output file; 0 divergent files satisfies the 100% byte-identical OUTCOME. |
| FR-041 | IMPLEMENTED | `src/registry/catalog.ts:loadCatalogOrFallback` + `CatalogRegistrySource` implement both the validation path (Zod schema parse with `safeParse`) and the fallback to built-in registry; `tests/contract/catalog-fallback.test.ts` covers unreachable, invalid, and valid catalog paths. Evidence kind: `unit_test`. | FR-041 is explicitly Nice-to-Have / SHOULD priority. The implementation satisfies all stated behaviors: validate before use, fall back when unavailable. The coverage-map "deferred-automation" note applies to future automation pipeline integration, not to the current feature completeness. |
| NFR-001 | IMPLEMENTED | `src/lifecycle/no-op-detect.ts:changedFileCount` + `isIdempotentNoOp`; `src/lifecycle/executor.ts` uses these to suppress writes when `changeType === 'unchanged'`; `tests/integration/lifecycle.test.ts:"AC-004/NFR-001"` confirms `changedFiles === 0` and byte-identical content on re-apply; `tests/e2e/cross-env-byte-identity.test.ts` measured artifact `test-results/cross-env-nfr007.json` (510/510 identical, `pass:true`) corroborates byte-identity at runtime scale. Evidence kind: `source_and_test` (integration) + `measured_runtime` (NFR-007 artifact overlap). | NFR-001 acceptance signal is 100% byte-identical + 0 changed files. Both the integration test (behavioral) and the measured cross-env artifact (runtime scale) satisfy this. |
| NFR-005 | IMPLEMENTED | `tests/e2e/perf-100x30.test.ts` runs `apply` over 100 artifacts × 30 targets and asserts `elapsedMs < 30_000`; measured CI artifact `test-results/benchmark-nfr005.json` records `elapsedMs: 954`, `thresholdMs: 30000`, `outputCount: 3000`, `pass: true`. Evidence kind: `measured_runtime`. | 954ms vs 30,000ms threshold. Measured artifact committed at 74fcf45. Threshold satisfied with large margin. |
| NFR-007 | IMPLEMENTED | `tests/e2e/cross-env-byte-identity.test.ts` applies over identical inputs in two independent temp roots and compares SHA-256 hashes file-by-file; measured CI artifact `test-results/cross-env-nfr007.json` records `fileCount: 510`, `identicalCount: 510`, `divergentCount: 0`, `pass: true`. Evidence kind: `measured_runtime`. | Artifact committed at 74fcf45. Two-environment run with distinct temp dirs and matching seeded corpus; 0 divergent files across 510 outputs. |
| NFR-009 | IMPLEMENTED | `tests/e2e/deterministic-render.test.ts` applies over identical inputs in two independent temp roots, hashes outputs, and compares against a committed golden snapshot; measured CI artifact `test-results/deterministic-render-nfr009.json` records `fileCount: 510`, `identicalCount: 510`, `divergentCount: 0`, `goldenMismatchCount: 0`, `pass: true`. Evidence kind: `measured_runtime`. | Artifact committed at 74fcf45. Deterministic field ordering in `src/render/order.ts` is the enabling mechanism; the measured artifact proves the threshold at runtime scale. |
| NFR-012 | IMPLEMENTED | `src/write/guarded-fs.ts:writeFileAtomic` (lines 71-78) writes to a PID+counter-suffixed temp file in the same directory then calls `fs.renameSync` — a single POSIX atomic operation; `src/manifest/manifest.ts:save` calls `this.fsGate.writeFileAtomic`; `tests/safety/manifest.test.ts:"NFR-012: the manifest write is atomic"` verifies byte-identical re-serialization and that no partial file state is observable. Evidence kind: `source_and_test`. | The temp-plus-rename pattern satisfies the spec's acceptance signal (no partially written manifests under interrupted or concurrent runs) via POSIX rename() atomicity. The safety test confirms correct behavior; the "exactly 1 atomic operation" constraint maps directly to the single `renameSync` call. |
