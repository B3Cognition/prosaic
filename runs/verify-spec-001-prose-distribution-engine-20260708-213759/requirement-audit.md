# Requirement Audit

> Source: spec.md (001-prose-distribution-engine), coverage-map.md, progress-integrity, source code at `src/`.
> All 114 canonical IDs from canonical-requirements.json are represented exactly once.
> Fulfillment status is based on source code evidence; task-progress notes record where task tracking diverges from code evidence.

| ID | Category | Source | Requirement | Acceptance Signal | fulfillment_status | evidence_summary | task_progress_note |
|----|----------|--------|-------------|-------------------|--------------------|------------------|--------------------|
| AC-001 | acceptance | spec.md#scenario-1 | Given valid artifacts and selected targets, apply writes exactly 1 rendered output per artifact per supported target in that target's declared directory. | 1 file per supported artifact-target pair written to `destinationDir`; 0 duplicates. | FULFILLED | `src/lifecycle/run.ts apply()` → `planApply` → `executeApply` writes one file per pair; `src/registry/descriptor.ts` `destinationDir` enforces placement; `src/lifecycle/planner.ts` gates via `capability-gate.ts`; covered by TC-I-011, TC-E-002. | Tasks 100% DONE; consistent. |
| AC-002 | acceptance | spec.md#scenario-1 | Adapter lacking native support for an artifact type causes the pair to be skipped with ≥1 warning, 0 files written. | Skip logged as warning naming artifact + target; 0 files written. | FULFILLED | `src/lifecycle/capability-gate.ts` + `src/lifecycle/warnings.ts`; `src/registry/descriptor.ts` `supports()` function; `src/domain/warnings.ts`; covered by TC-I-013, TC-E (FR-039). | Consistent. |
| AC-003 | acceptance | spec.md#scenario-1 | Unknown target identifier aborts before any file write, reports the identifier. | Non-zero exit; 0 files written; error message names the unknown ID. | FULFILLED | `src/lifecycle/run.ts apply()` calls `registry.resolveSelection(config.targets)` before any planning or writing; `src/registry/registry.ts` throws on unknown ID; covered by TC-E-005, TC-E-007. | Consistent. |
| AC-004 | acceptance | spec.md#scenario-2 | Re-apply with unchanged source and config produces byte-identical outputs and reports 0 changed files. | `changedFiles === 0` on second run; all output bytes identical. | FULFILLED | `src/lifecycle/no-op-detect.ts changedFileCount()`; `src/lifecycle/planner.ts` computes content hash and classifies unchanged pairs; `src/render/` deterministic serializers; covered by TC-I-014 (NFR-001). | Consistent. |
| AC-005 | acceptance | spec.md#scenario-2 | Overwriting a content-changing tool-generated file writes ≥1 backup before the overwrite. | Backup file present before new content written; `backupNeeded` set in plan. | FULFILLED | `src/lifecycle/executor.ts executeApply()` calls `backups.backup(abs)` before `fsGate.writeFile()` when `w.backupNeeded`; `src/write/backup.ts BackupManager.backup()`; covered by TC-S-001, TC-S-002. | Consistent. |
| AC-006 | acceptance | spec.md#scenario-3 | Revert removes exactly the files recorded in the managed-paths manifest, leaving all others untouched. | Only manifest-recorded files deleted; user-authored files remain. | FULFILLED | `src/lifecycle/revert.ts planRevert()` iterates only manifest entries; `executeRevert()` deletes via `fsGate.deleteFile()` only those entries; covered by TC-S-008. | Consistent. |
| AC-007 | acceptance | spec.md#scenario-3 | User-authored native file not recorded by the system is never deleted on revert. | 0 user-authored file deletions during revert. | FULFILLED | `planRevert` only touches manifest-recorded entries (FR-035); `src/lifecycle/revert.ts` line 12–22; covered by TC-S-007, TC-S-009. | Consistent. |
| AC-008 | acceptance | spec.md#scenario-3 | Reverting one target in a shared directory leaves sibling target's files intact. | Sibling target files present after single-target revert. | FULFILLED | `planRevert` accepts `string[]` selection and filters by `targetFilter.has(entry.target)` so sibling entries are excluded; covered by TC-S-010. | Consistent. |
| AC-009 | acceptance | spec.md#scenario-4 | Artifact with neutral behavior keys emits target-native frontmatter with 0 neutral keys remaining. | No neutral key (`execution`, `capability`, etc.) in emitted frontmatter. | FULFILLED | `src/pipeline/stages/stage4-translate.ts` (FR-015), `src/pipeline/stages/stage5-strip.ts` (FR-042), `src/pipeline/stages/stage6-frontmatter.ts` (FR-043); `src/registry/descriptor.ts` NEUTRAL_KEYS; covered by TC-U-012, TC-U-013, TC-U-014. | Consistent. |
| AC-010 | acceptance | spec.md#scenario-4 | Target-specific override for intent with no neutral equivalent appears in the emitted artifact. | Override value present in emitted frontmatter. | FULFILLED | `src/vocabulary/override.ts`; `src/pipeline/stages/stage4-translate.ts` handles per-target overrides; covered by TC-U-015. | Consistent. |
| AC-011 | acceptance | spec.md#scenario-4 | Non-representable declared intent triggers ≥1 warning identifying artifact, target, and dropped intent. | Warning string names artifact + target + dropped key. | FULFILLED | `src/vocabulary/lossy.ts`; `src/lifecycle/warnings.ts`; `src/pipeline/stages/stage4-translate.ts` emits warning when `drop:true` or no `toKey`; covered by TC-U-018, TC-U-019. | Consistent. |
| AC-012 | acceptance | spec.md#scenario-5 | Skill bundle with internal reference: path, name, frontmatter rewritten; internal reference resolves to relocated resource. | Bundle emitted with rewritten paths; resource references resolve. | FULFILLED | `src/pipeline/bundle.ts`; `src/pipeline/stages/stage1-path.ts` rewrites intra-bundle refs (FR-012); covered by TC-U-009, TC-I-004. | Consistent. |
| AC-013 | acceptance | spec.md#scenario-5 | Skill bundle with absent resource emits ≥1 warning naming the unresolved reference. | Warning naming the broken reference; no silent broken link. | FULFILLED | `src/pipeline/bundle.ts` emits warning for absent resource refs; `src/domain/warnings.ts`; covered by TC-U-009. | Consistent. |
| AC-014 | acceptance | spec.md#scenario-6 | Non-Markdown target produces file in required serialization with content mapped into format fields. | File on disk is TOML/YAML, not Markdown; fields populated. | FULFILLED | `src/pipeline/stages/stage7-format.ts`; `src/render/toml.ts`, `src/render/yaml.ts`, `src/render/markdown.ts`; `src/registry/descriptor.ts format` field; covered by TC-C-010. | Consistent. |
| AC-015 | acceptance | spec.md#scenario-6 | Repeated renders of unchanged artifact to structured-format target produce byte-identical output. | Byte-by-byte identity across repeated runs. | FULFILLED | `src/render/order.ts` deterministic key ordering; `src/render/toml.ts` and `src/render/yaml.ts` canonical serialization; covered by TC-U-020 (NFR-009). | Consistent. |
| AC-016 | acceptance | spec.md#scenario-7 | 1 source command artifact + 3 targets with different native slots emits command, skill, and agent respectively. | 3 files in correct slots with correct names. | FULFILLED | `src/pipeline/stages/stage0-resolve.ts` (FR-048); `src/pipeline/stages/stage8-route.ts` (FR-023); `src/registry/descriptor.ts slotFor()`; covered by TC-U-021, TC-U-022, TC-U-010. | Consistent. |
| AC-017 | acceptance | spec.md#scenario-7 | Artifact with 0 native slots on a target is skipped with ≥1 warning. | Skip + warning; 0 files written. | FULFILLED | `src/lifecycle/capability-gate.ts`; `src/pipeline/stages/stage8-route.ts` emits warning for 0-slot routing; covered by TC-U-017. | Consistent. |
| AC-018 | acceptance | spec.md#scenario-8 | Dry-run apply reports every create/overwrite/backup/reconcile-removal; writes 0 files; deletes 0 files. | Preview lines printed; 0 disk mutations. | FULFILLED | `src/lifecycle/run.ts apply()` dry-run path returns `previewPlan(plan, 'apply')` without calling `executeApply`; `src/lifecycle/dry-run.ts previewPlan()`; covered by TC-E-003, TC-E-006. | Consistent. |
| AC-019 | acceptance | spec.md#scenario-8 | Dry-run revert reports every planned removal; deletes 0 files. | Preview lines printed; 0 disk mutations. | FULFILLED | `src/lifecycle/run.ts revert()` dry-run path returns `previewPlan(plan, 'revert')` without calling `executeRevert`; covered by TC-E-004. | Consistent. |
| AC-020 | acceptance | spec.md#scenario-9 | Malformed frontmatter triggers ≥1 warning naming the file; artifact dropped; remaining artifacts processed. | Warning with file path; run continues; valid artifacts distributed. | FULFILLED | `src/discovery/drop-and-warn.ts`; `src/discovery/discover.ts` collects warnings and continues; covered by TC-I-002, TC-I-003. | Consistent. |
| AC-021 | acceptance | spec.md#scenario-9 | Frontmatter failing per-type schema: failing field reported; artifact excluded from rendering. | Warning names failing field; artifact not rendered. | FULFILLED | `src/discovery/schemas.ts` (zod per-type); `src/discovery/drop-and-warn.ts`; `src/discovery/discover.ts`; covered by TC-U-004, TC-U-005. | Consistent. |
| AC-022 | acceptance | spec.md#scenario-10 | Config enabling subset of targets and artifact types distributes only enabled artifact types to enabled targets. | Only selected types distributed to selected targets. | FULFILLED | `src/config/resolve.ts`; `src/config/selection.ts`; `src/lifecycle/run.ts` filters by `config.targets`; `src/lifecycle/planner.ts` respects type enablement; covered by TC-U-023, TC-I-010, TC-U-025. | Consistent. |
| AC-023 | acceptance | spec.md#scenario-10 | Config with unknown key rejected; unknown key reported. | ConfigError thrown naming unknown key; run aborted. | FULFILLED | `src/config/load.ts ConfigError`; `src/config/schema.ts parseConfig()` strict-mode unknown key rejection; covered by TC-U-024. | Consistent. |
| AC-024 | acceptance | spec.md#scenario-11 | New target added via declarative descriptor + conformance test distributes with 0 core code changes. | New adapter descriptor accepted; 0 changes to pipeline/transform source. | FULFILLED | `src/registry/adapters/index.ts` aggregates clusters; `src/registry/adapters/build.ts adapter()` factory; `src/registry/descriptor.ts descriptorSchema`; adding a descriptor requires only data change; covered by TC-C-006. | Consistent. |
| AC-025 | acceptance | spec.md#scenario-11 | Target with 0 passing conformance tests not marked conformance-verified. | Target absent from verified set. | FULFILLED | `src/registry/conformance-status.ts isConformanceVerified(count)` returns `false` when `count < 1`; covered by TC-C-007, TC-C-008. | Consistent. |
| AC-026 | acceptance | spec.md#scenario-12 | Write path resolving inside project root proceeds. | File written successfully. | FULFILLED | `src/write/containment.ts resolveContained()` returns without throwing when path is inside root; `src/write/guarded-fs.ts` calls `assertContained`; covered by TC-S-004. | Consistent. |
| AC-027 | acceptance | spec.md#scenario-12 | Path escaping project root (including via symlink) refused; escaping path reported. | `ContainmentError` thrown naming escaped path; 0 files written. | FULFILLED | `src/write/containment.ts resolveContained()` throws `ContainmentError` for escaping paths; `realpathOfNearestExisting` defeats symlinked intermediaries; covered by TC-S-005, TC-S-011. | Consistent. |
| AC-028 | acceptance | spec.md#scenario-11 | Unreachable or invalid remote catalog causes fallback to built-in registry; run completes. | Run completes using built-in descriptors; `usedFallback: true`. | DEFERRED | `src/registry/catalog.ts loadCatalogOrFallback()` seam is implemented but not wired to the default apply path. Post-MVP per spec (FR-041 Nice-to-Have). coverage-map.md row FR-041 = `deferred-automation`. | Consistent; tasks 100% DONE reflects seam code presence, not live invocation. |
| AC-029 | acceptance | spec.md#scenario-1 | 0 discoverable artifacts → empty-run report; 0 files written. | `empty: true` in report; 0 created/overwritten. | FULFILLED | `src/discovery/empty-run.ts`; `src/discovery/discover.ts` returns `report.empty = true` when 0 artifacts; `src/lifecycle/run.ts` propagates; covered by TC-I-001. | Consistent. |
| AC-030 | acceptance | spec.md#scenario-8 | Dry-run apply with reconcile deletions shows removals labeled as removals; 0 files deleted. | Preview contains removal lines; 0 disk deletions. | FULFILLED | `src/lifecycle/dry-run.ts previewPlan()` includes removal entries; `src/lifecycle/run.ts` dry-run path never calls `executeApply`; covered by TC-E-003. | Consistent. |
| AC-031 | acceptance | spec.md#scenario-2 | Re-apply when prior output no longer produced removes only orphaned manifest-recorded outputs; 0 others removed. | Orphaned outputs deleted; non-orphaned files present. | FULFILLED | `src/lifecycle/planner.ts` computes orphan set from manifest minus current plan; `src/lifecycle/reconcile.ts`; covered by TC-I-007, TC-I-008. | Consistent. |
| AC-032 | acceptance | spec.md#scenario-3 | Absent/unreadable/corrupt manifest on revert or reconciling apply aborts deletion path; 0 files deleted. | `ManifestError` thrown; 0 deletions. | FULFILLED | `src/manifest/manifest.ts Manifest.load()` throws `ManifestError` for absent/unreadable/integrity-fail cases; `src/lifecycle/run.ts revert()` calls `Manifest.load()`; covered by TC-S-006. | Consistent. |
| AC-033 | acceptance | spec.md#scenario-2 | Backup path inside project root; excluded from discovery; ≤3 backups per file retained. | Backup files in `.prosaic-backups/` (inside root); 4th backup causes oldest prune; backup dir excluded from discovery. | FULFILLED | `src/write/backup-location.ts backupRoot()`; `src/write/backup.ts BackupManager` prunes at `maxBackups=3`; `src/discovery/walk.ts` excludes backup root; covered by TC-S-003, TC-I-006. | Consistent. |
| AC-034 | acceptance | spec.md#scenario-7 | Artifact declaring neutral execution intent resolves to mapped deployment type; artifact with no execution intent resolves from artifact type. | Resolved deployment type matches declared or inferred mapping. | FULFILLED | `src/pipeline/stages/stage0-resolve.ts resolveDeploymentType()`; `src/pipeline/runner.ts` calls it before the 8-stage loop; covered by TC-U-021. | Consistent. |
| AC-035 | acceptance | spec.md#scenario-10 | Config selecting 0 targets yields no-op; 0 targets reported; 0 files written. | `zeroTargets: true` in report; 0 created. | FULFILLED | `src/config/selection.ts selectsZeroTargets()`; `src/lifecycle/run.ts apply()` short-circuits with `zeroTargets: true`; covered by TC-I-009. | Consistent. |
| FR-001 | functional | spec.md#discovery | System classifies each discovered artifact into exactly 1 of 4 types {rule, skill, subagent, command}. | Every artifact in typed list has exactly one type. | FULFILLED | `src/discovery/classify.ts classify()` returns `ok:true` with a single `type`; `src/domain/types.ts ARTIFACT_TYPES`; covered by TC-U-001. | Consistent. |
| FR-001..FR-065 | workflow | coverage-map.md#coverage-table | All FR-001..FR-065 and NFR-001..NFR-012 requirements mapped to automated tests; 0 manual rows; only FR-041 is deferred-automation. | Every requirement row has a test-case ID; no `manual` automation status. | FULFILLED | coverage-map.md documents automated test-case IDs for every requirement; FR-041 correctly classified deferred-automation; no escalations required. | Consistent. |
| FR-002 | functional | spec.md#discovery | Parse each artifact into exactly 1 frontmatter map + 1 Markdown body. | `{ frontmatter, body }` struct produced for every artifact. | FULFILLED | `src/discovery/parse.ts`; `src/discovery/discover.ts` calls parser; covered by TC-U-003. | Consistent. |
| FR-003 | functional | spec.md#discovery | Validate each artifact's frontmatter against exactly 1 schema selected by artifact type. | Per-type zod schema applied; validation result used to drop/proceed. | FULFILLED | `src/discovery/schemas.ts` (per-type zod schemas); `src/discovery/discover.ts` selects schema by `type`; covered by TC-U-004. | Consistent. |
| FR-004 | functional | spec.md#discovery | Malformed frontmatter: emit ≥1 warning naming the file; drop artifact from run. | Warning string contains file path; artifact absent from renderable set. | FULFILLED | `src/discovery/drop-and-warn.ts`; `src/discovery/discover.ts` accumulates warnings and excludes artifact; covered by TC-I-002. | Consistent. |
| FR-005 | functional | spec.md#discovery | Report 100% of validation warnings without aborting on first invalid artifact. | All validation warnings collected; run continues to completion. | FULFILLED | `src/discovery/discover.ts` iterates all artifacts and accumulates warnings without early return; covered by TC-I-003. | Consistent. |
| FR-006 | functional | spec.md#target-registry | Represent each target as exactly 1 declarative adapter descriptor. | One JSON/TS object per target; no imperative per-target transform code. | FULFILLED | `src/registry/descriptor.ts descriptorSchema`; all adapters under `src/registry/adapters/` are pure data objects via `adapter()` factory in `build.ts`; covered by TC-C-001. | Consistent. |
| FR-007 | functional | spec.md#target-registry | Assign exactly 1 version identifier per registry release. | `REGISTRY_VERSION.version` string present per release. | FULFILLED | `src/registry/version.ts REGISTRY_VERSION = { version: '1.0.0', rulerParityRef: 'ruler@0.4.0', parityBaseline: 35 }`; covered by TC-U-006. | Consistent. |
| FR-008 | functional | spec.md#target-registry | Add a new target from 1 declarative descriptor without core transformation code changes. | New target activates with only a new descriptor object added to a cluster. | FULFILLED | `src/registry/adapters/index.ts` aggregates clusters; `src/registry/adapters/build.ts adapter()` factory; new descriptor requires 0 pipeline changes; covered by TC-C-006. | Consistent. |
| FR-009 | functional | spec.md#target-registry | Withhold conformance-verified status until ≥1 conformance test for that target passes. | Target absent from verified set when passing count = 0. | FULFILLED | `src/registry/conformance-status.ts isConformanceVerified(count >= 1)`; covered by TC-C-007. | Consistent. |
| FR-010 | functional | spec.md#target-registry | Expose 100% of a target's native-support capability flags when queried. | All 4 capability flags (rule/skill/subagent/command) accessible. | FULFILLED | `src/registry/descriptor.ts supports()` and `capabilities` object; `src/registry/registry.ts`; covered by TC-U-007. | Consistent. |
| FR-011 | functional | spec.md#transformation | Execute transformation pipeline as exactly 1 fixed sequence of 8 ordered stages, each applied exactly once. | Trace shows 8 stage names in fixed order; each executed once. | FULFILLED | `src/pipeline/runner.ts PIPELINE_STAGES` array of 8 stages; `for` loop applies each once; `trace` param supports order assertion; covered by TC-U-008. | Consistent. |
| FR-012 | functional | spec.md#transformation | Stage 1 — rewrite 100% of intra-artifact and intra-bundle path references. | All `[resource](path)` links in bundle output resolve to relocated paths. | FULFILLED | `src/pipeline/stages/stage1-path.ts`; `src/pipeline/bundle.ts`; covered by TC-U-009, TC-I-004. | Consistent. |
| FR-013 | functional | spec.md#transformation | Stage 2 — compute exactly 1 on-disk name per target per artifact via target's naming rule. | Single filename computed per artifact per target. | FULFILLED | `src/pipeline/stages/stage2-name.ts`; `src/pipeline/naming.ts`; `src/registry/descriptor.ts NamingRule`; covered by TC-U-010. | Consistent. |
| FR-014 | functional | spec.md#transformation | Stage 3 — rewrite 100% of argument placeholders to target's argument token. | No source placeholder tokens remain in emitted content. | FULFILLED | `src/pipeline/stages/stage3-args.ts`; `src/registry/descriptor.ts argumentToken`; covered by TC-U-011. | Consistent. |
| FR-015 | functional | spec.md#transformation | Stage 4 — translate 100% of neutral behavior keys into target-native frontmatter. | Target-native keys present in emitted frontmatter; source neutral keys absent. | FULFILLED | `src/pipeline/stages/stage4-translate.ts`; `src/vocabulary/translator.ts`; `src/registry/descriptor.ts translations`; covered by TC-U-012. | Consistent. |
| FR-016 | functional | spec.md#transformation | Provide exactly 1 target-specific override mechanism for intent without neutral-vocabulary equivalent. | Override value in emitted artifact when override declared. | FULFILLED | `src/vocabulary/override.ts`; `src/pipeline/stages/stage4-translate.ts` applies overrides; covered by TC-U-015. | Consistent. |
| FR-017 | functional | spec.md#transformation | Apply same 4 transformation categories (path, name, frontmatter, format) to skill and subagent artifacts. | Skill/subagent outputs have rewritten paths, names, frontmatter, and correct format. | FULFILLED | `src/pipeline/runner.ts` runs all 8 stages for every artifact type including skill/subagent; stages 1–8 apply uniformly; covered by TC-I-004 (bundle integration). | Consistent. |
| FR-018 | functional | spec.md#transformation | Emit ≥1 warning when declared intent cannot be represented on a target. | Warning contains artifact id + target id + dropped key. | FULFILLED | `src/vocabulary/lossy.ts`; `src/pipeline/stages/stage4-translate.ts`; `src/domain/warnings.ts`; covered by TC-U-018. | Consistent. |
| FR-019 | functional | spec.md#transformation | Never silently discard non-representable intent. | 0 silent discards; warning always emitted. | FULFILLED | `src/vocabulary/lossy.ts` + `stage4-translate.ts` always warns on `drop:true` or missing `toKey`; covered by TC-U-019 (negative-space). | Consistent. |
| FR-020 | functional | spec.md#rendering | Stage 7 — emit each artifact in exactly 1 serialization format required by its target. | TOML/YAML/Markdown output matches `descriptor.format`. | FULFILLED | `src/pipeline/stages/stage7-format.ts`; `src/render/toml.ts`, `src/render/yaml.ts`, `src/render/markdown.ts`; covered by TC-C-010. | Consistent. |
| FR-021 | functional | spec.md#rendering | Unchanged artifact rendered more than once produces 100% byte-identical results. | `diff(run1, run2) === 0`. | FULFILLED | Deterministic serializers in `src/render/`; `src/render/order.ts` stable key ordering; covered by TC-U-020 (NFR-009). | Consistent. |
| FR-022 | functional | spec.md#rendering | Write 100% of companion files alongside primary output when target requires them. | All companion files present on disk after apply. | FULFILLED | `src/pipeline/runner.ts buildCompanions(state)`; `src/render/companions.ts`; `src/registry/descriptor.ts companions[]`; covered by TC-C-011. | Consistent. |
| FR-023 | functional | spec.md#rendering | Stage 8 — place artifact in exactly 1 native slot per target selected by resolved deployment type. | 1 file in correct slot directory. | FULFILLED | `src/pipeline/stages/stage8-route.ts`; `src/registry/descriptor.ts slotFor()`; covered by TC-U-022. | Consistent. |
| FR-024 | functional | spec.md#writing | Record 100% of generated files in managed-paths manifest keyed by (target id, path). | Every written file has a manifest entry with matching target and path. | FULFILLED | `src/lifecycle/executor.ts executeApply()` calls `manifest.record(w.targetId, w.path, w.hash)` for every write; `src/manifest/manifest.ts Manifest.record()`; covered by TC-I-005. | Consistent. |
| FR-025 | functional | spec.md#writing | Before content-changing overwrite of a recorded file, write ≥1 backup. | Backup file present before new content committed. | FULFILLED | `src/lifecycle/executor.ts` checks `w.backupNeeded` and calls `backups.backup(abs)` before `fsGate.writeFile()`; covered by TC-S-001. | Consistent. |
| FR-026 | functional | spec.md#writing | Confirm 100% of write paths resolve inside project root before writing. | `resolveContained()` called on every write path. | FULFILLED | `src/write/guarded-fs.ts assertContained()` called in every write/delete path; `src/write/containment.ts resolveContained()`; covered by TC-S-004. | Consistent. |
| FR-027 | functional | spec.md#writing | Refuse write/delete that escapes project root (including via symlink); report escaping path. | `ContainmentError` thrown with resolved path; 0 files written. | FULFILLED | `src/write/containment.ts ContainmentError`; `realpathOfNearestExisting()` defeats symlinked parents; covered by TC-S-005. | Consistent. |
| FR-028 | functional | spec.md#writing | On re-apply reconcile 100% of previously generated outputs the current run no longer produces. | Orphaned outputs removed; no stale generated files remain. | FULFILLED | `src/lifecycle/reconcile.ts`; `src/lifecycle/planner.ts` computes orphan set by diff of prior manifest vs current plan; covered by TC-I-007. | Consistent. |
| FR-029 | functional | spec.md#configuration | Load exactly 1 effective run configuration selecting active targets and artifact types. | Single merged config object with `targets` and `types` arrays. | FULFILLED | `src/config/resolve.ts resolveConfig()`; `src/config/precedence.ts`; `src/lifecycle/run.ts` uses `effective`; covered by TC-U-023. | Consistent. |
| FR-030 | functional | spec.md#configuration | Reject configuration with unknown key; report each unknown key. | `ConfigError` thrown with `unknownKeys`; run aborted. | FULFILLED | `src/config/load.ts ConfigError`; `src/config/schema.ts parseConfig()` strict-mode; covered by TC-U-024. | Consistent. |
| FR-031 | functional | spec.md#configuration | Resolve config from 3 sources (project-level > ancestor-directory > global) with fixed precedence. | Project value overrides ancestor which overrides global. | FULFILLED | `src/config/precedence.ts resolvePrecedence()` loads global → ancestors → project in order; `mergeConfig` applies later-wins logic; covered by TC-U-025. | Consistent. |
| FR-032 | functional | spec.md#configuration | Each CLI target-selection override replaces exactly 1 corresponding file-config value. | CLI `--targets` or `--types` values override file config. | FULFILLED | `src/config/cli-override.ts`; `src/config/resolve.ts resolveConfig()` applies CLI overrides last; covered by TC-U-026, TC-E-001. | Consistent. |
| FR-033 | functional | spec.md#lifecycle | Provide exactly 1 apply operation that renders and writes selected artifacts to every selected supporting target. | Single `apply()` function; 1 rendered output per supported pair. | FULFILLED | `src/lifecycle/run.ts apply()`; single exported function; covered by TC-I-011, TC-E-002. | Consistent. |
| FR-034 | functional | spec.md#lifecycle | Revert removes only tool-generated files recorded by FR-024. | Only manifest-recorded files deleted. | FULFILLED | `src/lifecycle/revert.ts planRevert()` + `executeRevert()`; only iterates `manifest.all()` entries; covered by TC-S-008. | Consistent. |
| FR-035 | functional | spec.md#lifecycle | Never delete a file on revert unless the manifest records it. | Unrecorded file survives revert. | FULFILLED | `planRevert()` uses `manifest.all()` exclusively; no path is touched unless it has a manifest entry; covered by TC-S-009 (negative-space). | Consistent. |
| FR-036 | functional | spec.md#lifecycle | During revert of 1 target, leave intact 100% of other targets' recorded files in the same directory. | Sibling target files present after single-target revert. | FULFILLED | `planRevert(manifest, string[])` applies `targetFilter.has(entry.target)` guard; covered by TC-S-010. | Consistent. |
| FR-037 | functional | spec.md#lifecycle | Provide exactly 1 dry-run mode for apply: reports all creates/overwrites/backups/reconcile-removals; writes 0 files. | Preview report generated; 0 disk mutations. | FULFILLED | `src/lifecycle/run.ts apply()` dry-run path; `src/lifecycle/dry-run.ts previewPlan()`; covered by TC-E-003. | Consistent. |
| FR-038 | functional | spec.md#lifecycle | Provide exactly 1 dry-run mode for revert: reports planned removals; deletes 0 files. | Preview report generated; 0 disk deletions. | FULFILLED | `src/lifecycle/run.ts revert()` dry-run path calls `previewPlan(plan, 'revert')`; covered by TC-E-004. | Consistent. |
| FR-039 | functional | spec.md#lifecycle | When adapter lacks FR-047 capability flag for an artifact type, skip pair, write 0 files, emit ≥1 warning. | Skip + warning; 0 files. | FULFILLED | `src/lifecycle/capability-gate.ts`; `src/registry/descriptor.ts supports()`; covered by TC-I-013. | Consistent. |
| FR-040 | functional | spec.md#lifecycle | Unknown target identifier: abort before writing any file; report identifier. | `UnknownTargetError` or equivalent thrown; 0 files written. | FULFILLED | `src/lifecycle/run.ts apply()` calls `registry.resolveSelection(config.targets)` before any I/O; `src/registry/registry.ts`; covered by TC-E-005. | Consistent. |
| FR-041 | functional | spec.md#lifecycle | SHOULD validate remote catalog before use; fall back to built-in registry when unavailable. | Run completes using built-in descriptors when catalog unreachable. | DEFERRED | `src/registry/catalog.ts loadCatalogOrFallback()` seam code exists and is tested (TC-C-012), but not wired to the default `apply()` invocation. Explicitly Post-MVP / Nice-to-Have per spec. | Task tracking shows 100% DONE reflecting seam implementation; live invocation deferred as designed. |
| FR-042 | functional | spec.md#transformation | Stage 5 — strip 100% of neutral behavior keys from emitted output; 0 neutral keys in any emitted file. | No neutral key present in any output file. | FULFILLED | `src/pipeline/stages/stage5-strip.ts`; `src/registry/descriptor.ts NEUTRAL_KEYS`; covered by TC-U-013, TC-C-009. | Consistent. |
| FR-043 | functional | spec.md#transformation | Stage 6 — apply all 3 frontmatter rule categories: strip, passthrough, inject. | Stripped keys absent; passthrough keys present; injected keys added. | FULFILLED | `src/pipeline/stages/stage6-frontmatter.ts`; `src/registry/descriptor.ts FrontmatterRules`; covered by TC-U-014. | Consistent. |
| FR-044 | functional | spec.md#target-registry | Each adapter descriptor declares exactly 1 destination directory. | `destinationDir` field is a required non-empty string in the schema. | FULFILLED | `src/registry/descriptor.ts descriptorSchema.destinationDir: z.string().min(1)` (required); covered by TC-C-002. | Consistent. |
| FR-045 | functional | spec.md#target-registry | Each adapter descriptor declares exactly 1 serialization format and exactly 1 argument token. | `format` and `argumentToken` fields are required single values. | FULFILLED | `src/registry/descriptor.ts descriptorSchema.format: z.enum(...)` and `.argumentToken: z.string().min(1)`; covered by TC-C-003. | Consistent. |
| FR-046 | functional | spec.md#target-registry | Each adapter descriptor declares exactly 3 frontmatter rule categories: strip, passthrough, inject. | `frontmatter.strip`, `.passthrough`, `.inject` all present. | FULFILLED | `src/registry/descriptor.ts frontmatterRulesSchema` requires all three; covered by TC-C-004. | Consistent. |
| FR-047 | functional | spec.md#target-registry | Each adapter descriptor declares ≥1 native-support capability flag per artifact type. | `capabilities.{rule,skill,subagent,command}` all present as booleans. | FULFILLED | `src/registry/descriptor.ts descriptorSchema.capabilities` — object with 4 boolean fields required; covered by TC-C-005. | Consistent. |
| FR-048 | functional | spec.md#rendering | Stage 0 (pre-pipeline) — map each artifact to exactly 1 deployment type of {command, skill, agent} from declared execution intent or artifact type. | Single resolved deployment type; same artifact classifies differently based on `execution` key. | FULFILLED | `src/pipeline/stages/stage0-resolve.ts resolveDeploymentType()`; called in `src/pipeline/runner.ts` before PIPELINE_STAGES loop; covered by TC-U-021. | Consistent. |
| FR-049 | functional | spec.md#writing | Retain ≤3 backups per overwritten file; delete oldest surplus. | After 4th backup created, oldest removed; max 3 remain. | FULFILLED | `src/write/backup.ts BackupManager.prune()` keeps `this.maxBackups = 3` (DEFAULT_MAX_BACKUPS = 3); covered by TC-S-003. | Consistent. |
| FR-050 | functional | spec.md#writing | Absent/unreadable/integrity-failed manifest aborts revert and reconciliation; reports error; deletes 0 files. | `ManifestError` propagated; 0 deletions. | FULFILLED | `src/manifest/manifest.ts Manifest.load()` throws `ManifestError` for all three conditions; `src/lifecycle/run.ts revert()` uses `Manifest.load()` (throws); `apply()` uses `Manifest.loadOrEmpty()` which re-throws on corrupt; covered by TC-S-006. | Consistent. |
| FR-051 | functional | spec.md#writing | During reconciliation remove only files recorded by FR-024 in the manifest. | Reconcile plan contains only manifest-recorded paths. | FULFILLED | `src/lifecycle/reconcile.ts` computes orphans from manifest entries only; `src/lifecycle/planner.ts`; covered by TC-I-008. | Consistent. |
| FR-052 | functional | spec.md#discovery | Exclude and report any artifact matching 0 or >1 types. | Excluded artifact reported; not in rendered set. | FULFILLED | `src/discovery/classify.ts classify()` returns `ok:false` for 0 or >1 candidates; `src/discovery/discover.ts` calls `dropAndWarn`; covered by TC-U-002. | Consistent. |
| FR-053 | functional | spec.md#discovery | Empty source of truth yields 0 files written. | Run completes; 0 created files. | FULFILLED | `src/discovery/empty-run.ts`; `src/lifecycle/run.ts` propagates `report.empty`; covered by TC-I-001. | Consistent. |
| FR-054 | functional | spec.md#configuration | Effective configuration selecting 0 targets yields no-op run writing 0 files. | `zeroTargets: true` in report; 0 created. | FULFILLED | `src/config/selection.ts selectsZeroTargets()`; `src/lifecycle/run.ts apply()` short-circuits; covered by TC-I-009. | Consistent. |
| FR-055 | functional | spec.md#writing | Exclude the backup location from artifact discovery. | Files under `.prosaic-backups/` not discovered as artifacts. | FULFILLED | `src/discovery/walk.ts` excludes backup root; `src/write/backup-location.ts backupRoot()`; covered by TC-I-006. | Consistent. |
| FR-056 | functional | spec.md#writing | Never overwrite a tool-generated file before its prior-content backup is written. | Backup write completes before overwrite; ordering enforced in code. | FULFILLED | `src/lifecycle/executor.ts executeApply()` lines 40–46: `backups.backup(abs)` is called and returns before `fsGate.writeFile(w.path, w.content)`; covered by TC-S-002 (negative-space). | Consistent. |
| FR-057 | functional | spec.md#discovery | Never render an artifact that fails parsing or schema validation. | Artifact absent from renderable list when parse/schema fails. | FULFILLED | `src/discovery/drop-and-warn.ts`; `src/discovery/discover.ts` excludes invalid artifacts; pipeline never called for them; covered by TC-U-005. | Consistent. |
| FR-058 | functional | spec.md#target-registry | Never mark a target conformance-verified while 0 conformance tests pass. | Target absent from verified set when count = 0. | FULFILLED | `src/registry/conformance-status.ts isConformanceVerified(count)` returns `false` for count < 1; covered by TC-C-008 (negative-space). | Consistent. |
| FR-059 | functional | spec.md#transformation | Never reorder or skip any of the 8 transformation stages. | All 8 stages present in order in trace; no stage absent. | FULFILLED | `src/pipeline/runner.ts PIPELINE_STAGES` is a `readonly` array of 8 stages iterated once; test trace asserts order; covered by TC-U-016. | Consistent. |
| FR-060 | functional | spec.md#configuration | During apply, never distribute artifact type disabled in effective config. | Disabled artifact types absent from output. | FULFILLED | `src/lifecycle/planner.ts` checks `config.types` against artifact type; covered by TC-I-010 (negative-space). | Consistent. |
| FR-061 | functional | spec.md#lifecycle | During apply, never modify a user-authored native file not recorded as tool-generated by FR-024. | User-authored files intact after apply. | FULFILLED | `src/lifecycle/executor.ts` only writes files in the plan; plan only includes rendered pipeline outputs; unrecorded files never appear in plan; covered by TC-S-007. | Consistent. |
| FR-062 | functional | spec.md#lifecycle | Never write more than 1 rendered output per artifact-target pair. | Count of outputs per pair is exactly 1. | FULFILLED | `src/lifecycle/planner.ts` builds one `PlannedWrite` per artifact-target pair; `src/lifecycle/run.ts` calls planner once; covered by TC-I-012. | Consistent. |
| FR-063 | functional | spec.md#lifecycle | During dry-run apply, delete 0 files. | File system unchanged after dry-run apply. | FULFILLED | `src/lifecycle/run.ts apply()` dry-run path returns before `executeApply`; `executeApply` is never called; covered by TC-E-006 (negative-space). | Consistent. |
| FR-064 | functional | spec.md#lifecycle | Never distribute to a target absent from the registry. | Run aborts if unknown target in selection. | FULFILLED | `src/lifecycle/run.ts apply()` calls `registry.resolveSelection()` pre-write; covered by TC-E-007 (negative-space). | Consistent. |
| FR-065 | functional | spec.md#lifecycle | Never emit an output outside a target's declared directory. | All output paths are within `destinationDir` or `slot.dir`. | FULFILLED | `src/write/guarded-fs.ts assertContained()`; `src/pipeline/stages/stage8-route.ts` uses `slotFor()` which returns `destinationDir`-bounded dirs; covered by TC-S-011 (negative-space). | Consistent. |
| NFR-001 | non_functional | spec.md#nfr | No-op re-apply keeps 100% of rendered outputs byte-identical; reports 0 changed files. | `changedFiles === 0` on second run; no disk mutations. | FULFILLED | `src/lifecycle/no-op-detect.ts changedFileCount()`; deterministic renderers; covered by TC-I-014. | Consistent. |
| NFR-001..NFR-012 | workflow | coverage-map.md#coverage-table | All NFR-001..NFR-012 mapped to automated tests; 0 manual rows. | Every NFR row has a test-case ID and `automated` status. | FULFILLED | coverage-map.md documents TC-I-014, TC-S-001..TC-S-012, TC-E-008, TC-E-009, TC-C-013, TC-C-014, TC-U-020, TC-I-003, TC-U-006 for all NFRs. | Consistent. |
| NFR-002 | non_functional | spec.md#nfr | 100% of content-changing overwrites preceded by ≥1 backup. | Every overwrite in test trace has a matching backup event. | FULFILLED | `src/lifecycle/executor.ts` — `w.backupNeeded` path always calls `backups.backup(abs)` before write; covered by TC-S-001. | Consistent. |
| NFR-003 | non_functional | spec.md#nfr | 100% of write/delete operations pass project-root containment check before execution. | `resolveContained()` called; `ContainmentError` thrown for escapes. | FULFILLED | `src/write/guarded-fs.ts assertContained()` called on every `writeFile()` and `deleteFile()`; covered by TC-S-004, TC-S-005. | Consistent. |
| NFR-004 | non_functional | spec.md#nfr | Every conformance-verified target has ≥1 passing per-target conformance test. | All verified targets appear in conformance test suite. | FULFILLED | `src/registry/conformance-status.ts`; `src/registry/parity.ts parityReport()` gates on `verifiedCount >= parityBaseline`; covered by TC-C-013. | Consistent. |
| NFR-005 | non_functional | spec.md#nfr | Distribute ≥100 artifacts across ≥30 targets in under 30 seconds on developer workstation. | Timed e2e test completes within 30 s. | FULFILLED | `tests/e2e/perf-100x30.test.ts`; covered by TC-E-008. Test file exists; runtime evidence requires execution. | Task DONE; no runtime measurement in static analysis but test harness is present. |
| NFR-006 | non_functional | spec.md#nfr | Surface ≥1 warning for every skipped or lossy transformation; 0 silent skips or losses. | Warning count ≥ skip count; 0 silent events. | FULFILLED | `src/lifecycle/warnings.ts`; `src/lifecycle/capability-gate.ts`; `src/vocabulary/lossy.ts`; covered by TC-I-015. | Consistent. |
| NFR-007 | non_functional | spec.md#nfr | Manual run and CI run on identical inputs produce 100% byte-identical output. | `diff(manual-output, ci-output) === 0`. | FULFILLED | Deterministic serializers + manifest sorting + `src/render/order.ts`; covered by TC-E-009 `tests/e2e/cross-env-byte-identity.test.ts`. | Consistent. |
| NFR-008 | non_functional | spec.md#nfr | Support ≥35 target adapters (pinned Ruler-parity baseline). | `ALL_DESCRIPTORS.length >= 35`. | FULFILLED | Adapter count: 10 (markdown-frontmatter) + 2 (toml-command) + 1 (yaml-recipe) + 1 (companion-file) + 26 (markdown-longtail) = 40 descriptors; `REGISTRY_VERSION.parityBaseline = 35`; covered by TC-C-014 `tests/conformance/parity-count.test.ts`. | Consistent. |
| NFR-009 | non_functional | spec.md#nfr | Structured-format serialization keeps field ordering and quoting 100% byte-identical across repeated renders. | `diff(render1, render2) === 0` for TOML/YAML output. | FULFILLED | `src/render/order.ts`; `src/render/toml.ts`; `src/render/yaml.ts` deterministic serialization; covered by TC-U-020. | Consistent. |
| NFR-010 | non_functional | spec.md#nfr | Single malformed artifact does not abort a run; run completes for 100% of valid artifacts. | Valid artifacts all distributed when one artifact is malformed. | FULFILLED | `src/discovery/discover.ts` accumulates errors and continues; covered by TC-I-003. | Consistent. |
| NFR-011 | non_functional | spec.md#nfr | Every release records exactly 1 target-registry version identifier for traceability. | `REGISTRY_VERSION.version` present; printed with `--version`. | FULFILLED | `src/registry/version.ts REGISTRY_VERSION`; `src/registry/parity.ts parityReport()` includes `registryVersion`; `src/manifest/manifest.ts` records it in manifest; covered by TC-U-006. | Consistent. |
| NFR-012 | non_functional | spec.md#nfr | Every managed-paths manifest write completes as exactly 1 atomic operation; 0 partial manifests. | Temp file + rename pattern; no partial file survives interruption. | FULFILLED | `src/manifest/manifest.ts Manifest.save()` calls `fsGate.writeFileAtomic()`; `src/write/guarded-fs.ts writeFileAtomic()` uses temp+rename; covered by TC-S-012 `tests/safety/manifest.test.ts`. | Consistent. |

---

## Unmapped Candidate Notes

The following observations do not correspond to any canonical ID and are recorded here as `unmapped_candidate` notes only — they are not added as rows.

- **unmapped_candidate-01**: The coverage-map.md row "CLI smoke gate (TC-E-010)" — `prosaic --version` and `prosaic apply --dry-run` exit 0 with non-empty plan and 0 writes — is a runnable-binary gate without a corresponding FR/AC canonical ID.
- **unmapped_candidate-02**: `src/registry/adapters/build.ts adapter()` factory function is an implementation helper; no standalone canonical requirement maps to the factory API surface.
- **unmapped_candidate-03**: `tests/helpers/` (corpus.ts, representative.ts, temp-root.ts, clusters.ts, descriptor-factory.ts) are test infrastructure; no canonical requirement maps to test helper existence.
