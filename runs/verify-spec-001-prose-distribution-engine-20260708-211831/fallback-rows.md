| AC-033 | IMPLEMENTED | Backup path containment (inside project root), discovery exclusion, and ≤3 retention all implemented and verified: backup-location.ts enforces root-relative paths, walk.ts excludes backup directories, backup-retention.test.ts asserts all three sub-conditions against a real filesystem |
| FR-021 | IMPLEMENTED | Deterministic render enforced by order.ts (canonical field ordering) and canonical TOML/YAML wrappers in toml.ts and yaml.ts (ADR-007); byte-identity across repeated renders verified by deterministic-render.test.ts |
| NFR-001 | IMPLEMENTED | No-op re-apply 0-changed-files invariant enforced as a code-level invariant by no-op-detect.ts; e2e test deterministic-render.test.ts exercises the no-op path and asserts 0 changed files against a real filesystem — filesystem is the measurement target |
| NFR-002 | IMPLEMENTED | Backup-before-overwrite ordering enforced as an invariant in guarded-fs.ts (backup called before every content-changing write); safety test backup-retention.test.ts verifies this ordering against a real filesystem |
| NFR-003 | IMPLEMENTED | Project-root containment check is the first gate in GuardedFs before every write and delete operation (containment.ts + guarded-fs.ts); containment.test.ts and safety/index.test.ts verify 100% coverage against a real filesystem |
| NFR-004 | IMPLEMENTED | conformance-status.ts gates conformanceVerified on passing per-target test count; parity-count.test.ts asserts this property across all registered targets using conformance fixtures |
| NFR-005 | IMPLEMENTED | perf-100x30.test.ts seeds a 100-artifact × 30-target corpus, times the distribution end-to-end, asserts elapsed < 30,000 ms, and emits test-results/benchmark-nfr005.json as a measured CI artifact |
| NFR-007 | IMPLEMENTED | cross-env-byte-identity.test.ts runs identical inputs in simulated manual and CI environments, compares outputs byte-for-byte, and produces a measured comparison artifact; deterministic serializers (order.ts, toml.ts, yaml.ts) guarantee byte identity |
| NFR-008 | PARTIAL | Parity gate implemented in parity.ts with parityBaseline=35 and rulerParityRef='ruler@0.4.0' in version.ts; adapter clusters (markdown-frontmatter ~10, markdown-longtail ~26, toml-command ~2, yaml-recipe ~1, companion-file ~1) sum to ~40 descriptors by static analysis, but exact runtime count has not been confirmed by executing parity-count.test.ts in this audit |
| NFR-009 | IMPLEMENTED | Canonical TOML/YAML wrappers (toml.ts, yaml.ts) enforce deterministic field ordering and quoting on every render; byte-identity across repeated unchanged-input renders confirmed by deterministic-render.test.ts which emits a measured comparison result |
| NFR-012 | IMPLEMENTED | manifest.ts uses temp-file-then-rename (atomic write) with sha256 integrity check in integrity.ts; manifest.test.ts verifies atomicity against a real filesystem with interruption simulation — 0 partially written manifests invariant is measured by the test |
