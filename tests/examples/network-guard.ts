import * as path from 'path';

/** Absolute path to the `--require`-loadable network guard preload script. */
export const NETWORK_GUARD_PRELOAD_PATH = path.join(__dirname, 'network-guard-preload.js');
