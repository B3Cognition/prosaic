import * as path from 'path';

/** Absolute path to the `--require`-loadable filesystem-access guard preload script. */
export const FS_GUARD_PRELOAD_PATH = path.join(__dirname, 'fs-guard-preload.js');
